{
  "name": "jeremy.codes",
  "version": "2.0.0",
  "description": "My personal website.",
  "main": "./src/index.js",
  "scripts": {
    "clean": "npx rimraf dist",
    "compress": "npm run gzip && npm run brotli",
    "cssnano": "echo \"Minifying CSS...\" && find ./dist -type f -name '*.css' -exec npx cssnano {} {} \\;",
    "gzip": "echo \"Statically compressing assets (gzip)...\" && find -E ./dist -regex '.*\\.(m?js|css|svg|html)' -exec gzip -9k {} \\;",
    "brotli": "echo \"Statically compressing assets (brotli)...\" && find -E ./dist -regex '.*\\.(m?js|css|svg|html)' -exec brotli -o {}.br -k -q 11 {} \\;",
    "imageopt": "echo \"Optimizing images...\" && npm run imageopt:png && npm run imageopt:webp-png",
    "imageopt:webp-png": "echo \"Converting PNGs to lossless WebP...\" && find ./dist -type f -name '*.png' | xargs -P 8 -I {} sh -c 'npx cwebp -quiet -lossless -q 100 -z 9 -m 6 $1 -o \"${1%.png}.webp\"' _ {} \\;",
    "imageopt:png": "echo \"Optimizing PNGs...\" && find ./dist -type f -name '*.png' | xargs -P 8 -I {} sh -c 'npx pngquant --force --skip-if-larger --output $1 --speed 1 --strip $1' _ {} \\;",
    "analyze": "npm run clean && NODE_ENV=production npx webpack --profile --progress --json > bundle-stats.json && npx webpack-bundle-analyzer bundle-stats.json",
    "audit": "npm run build && npx bundle-buddy dist/js/*.map",
    "lint": "npx eslint --no-inline-config ./src/**/*.js"
  },
  "repository": {
    "type": "git",
    "url": "git+ssh://git@github.com/malchata/jeremy.codes.git"
  },
  "author": "Jeremy L. Wagner <jeremy.l.wagner@gmail.com>",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/malchata/jeremy.codes/issues"
  },
  "homepage": "https://github.com/malchata/jeremy.codes#readme",
  "dependencies": {
    "@babel/polyfill": "^7.2.5",
    "@babel/runtime": "^7.2.0",
    "preact": "^8.4.2",
    "preact-markup": "^1.6.0"
  },
  "devDependencies": {
    "@babel/cli": "^7.2.3",
    "@babel/core": "^7.2.2",
    "@babel/plugin-syntax-dynamic-import": "^7.2.0",
    "@babel/plugin-transform-runtime": "^7.2.0",
    "@babel/preset-env": "^7.2.3",
    "@babel/preset-react": "^7.0.0",
    "@babel/register": "^7.0.0",
    "babel-eslint": "^10.0.1",
    "cssnano": "^4.1.8",
    "cwebp-bin": "^5.0.0",
    "eslint": "^5.11.1",
    "eslint-plugin-react": "^7.12.0",
    "express": "^4.16.4",
    "pngquant-bin": "^5.0.1",
    "preact-render-to-string": "^4.1.0",
    "rimraf": "^2.6.2"
  }
}
